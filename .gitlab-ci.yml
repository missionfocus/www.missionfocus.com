# Full project: https://gitlab.com/pages/jekyll
image: ruby:2.3

test:
  stage: test
  script:
  - echo $(whoami)
  - gem install jekyll
  - jekyll build -d test
  artifacts:
    expire_in: 2 weeks
    paths:
    - test
  except:
  - master

pages:
  stage: deploy
  script:
  - echo $(whoami)
  - gem install jekyll
  - jekyll build -d public
  artifacts:
    expire_in: 2 weeks
    paths:
    - public
  only:
  - master
  